import vscode from "vscode";
import { IFileheaderVariables } from "./types";
export class FileheaderVariableBuilder {
  public build(config: vscode.WorkspaceConfiguration): IFileheaderVariables {
    return {
      // not implement
    } as any;
  }
}
